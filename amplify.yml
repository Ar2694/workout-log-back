version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npm install
    build:
      commands:
        - npm run build
    post_build:
      commands:
        - npm run serve
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'